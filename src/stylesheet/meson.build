pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())

stylesheet_sources = [
  'style.css',
  'style-dark.css',
  'style-hc.css',
  'launcher.css',
]

install_data(stylesheet_sources, install_dir: join_paths(pkgdatadir, 'aspinwall', 'stylesheets'))
