pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
moduledir = join_paths(pkgdatadir, 'aspinwall')
gnome = import('gnome')

subdir('launcher')
subdir('shell')
subdir('utils')
subdir('widgets')
