pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
widgetdir = join_paths(pkgdatadir, 'widgets')

widgets_data = [
  'org.dithernet.aspinwall.widgets.welcome',
]

install_subdir(widgets_data, install_dir: widgetdir)
